const express = require("express");

const app = express();

app.get()

app.listen(8000, (error) => {
  if (error) {
    console.log(error);
    return;
  }
  console.log(`server is running on port 8000`);
});
